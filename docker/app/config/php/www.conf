[www]
; Pass the container environment through to PHP-FPM workers. FPM clears the
; environment by default (clear_env = on), which would hide the variables that
; docker compose injects via `env_file`. The app reads all configuration from
; these real environment variables (no .env files), so workers must see them.
clear_env = no
