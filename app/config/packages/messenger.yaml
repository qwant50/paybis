framework:
    messenger:
        default_bus: messenger.bus.default
        buses:
            messenger.bus.default: ~
        # The Symfony Scheduler transport that drives the 5-minute rate fetch is
